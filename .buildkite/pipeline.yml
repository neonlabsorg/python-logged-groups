steps:
#  - label: "define if for full test suite"
#    command: buildkite-agent meta-data set "full_test_suite" "true"
#    agents:
#      queue: "testing"

  - label: "Echo meta evm_loader_branch"
    command: echo evm_loader_branch - $(buildkite-agent meta-data get "evm_loader_branch" --default "false")
    agents:
      queue: "testing"

  - label: "Echo env FULL_TEST_SUITE"
    command: echo "full_test_suite:" ${FULL_TEST_SUITE}
    agents:
      queue: "testing"